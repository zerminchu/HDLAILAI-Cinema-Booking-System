import { Table } from "@mantine/core";

function Seat({ booked }) {
  return <div></div>;
}

function Seats({noOfRow, noOfCol, seats = []}) {
    const generateSeats = seats.map()
    return (<div>
        <div></div>
    </div>)
}